---
layout: default
parent: Data Ingest
nav_order: 3
---

# Event Messaging

Event messaging allows the system to respond to events. For example, as new data arrives, consumers of that data can update their views as needed. This can reduce overhead lag, providing faster data availability.